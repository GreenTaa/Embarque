sudo apt update
sudo apt upgrade

sudo apt install mongodb // install package

sudo systemctl enable mongodb  // activate service
sudo systemctl start mongodb  // start service

Install pymongo :

sudo pip install pymongo
sudo pip install pymongo[srv] (important for mongoclient)


